build & create a debian package:
$ ./make_deb.sh

or

To compile:
$ make all

To install:
$ make install
or
$ PREFIX=/opt/xxx make install

UPDATE:

Falta marcar a dependencial do protoc -> protobuf-compiler

$./bootstrap
$./make_deb.sh